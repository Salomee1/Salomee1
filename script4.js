let colors = ['Red', 'Green', 'Blue']; 

if (colors.length > 0) {
  
  let firstColor = colors[0];

  1
  let lastColor = colors[colors.length - 1];

  
  alert("The first color is: " + firstColor);
  alert("The last color is: " + lastColor);
} else {
  
  alert("The color array is empty.");
}
